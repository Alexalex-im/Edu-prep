#include <stdio.h>
int sum_of_number (int first, int second);
int main()
{
    int first_number = 4; second_number = 7;
    int answer;

    answer = first_number + second_number;
    printf("The addition of the above two numbers id %d \n", answer);
    return 0;
}
int sum_of_numbers (int first, int second) {
    int sum = first + second;
    return sum;
}